# algorithms.txt
## Search
- Binary search is an efficient algorithm for finding an item from a sorted list of items.
- Binary search runs in logarithmic time O(log n), making it much faster than linear search for large
## Algorithm
- The merge sort algorithm is a divide-and-conquer sorting algorithm.
- Quick sort is another divide-and-conquer algorithm.
## Log
- Merge sort has a time complexity of O(n log n) in all cases and a space complexity of O(n).
- Quick sort has average time complexity of O(n log n) but worst-case of O(n^2).
## Left
- If the target is greater, you move left to middle + 1.
- You repeat this process until left exceeds right.
## Index
- You calculate the middle index and compare the value at that position with your target.
- If the middle value equals the target, you return the index.
## Middle
- If the target is smaller, you move right to middle - 1.
## Sort
- It divides the input array into two halves, recursively sorts each half, and then merges the sorted
## Problems
- Dynamic programming is a method for solving complex problems by breaking them down into simpler subp
- It is applicable to problems exhibiting overlapping subproblems and optimal substructure.
## Depth
- BFS explores all vertices at the present depth before moving to vertices at the next depth level, us
## Bfs
- BFS finds the shortest path in unweighted graphs.
## Item
- It works by repeatedly dividing in half the portion of the list that could contain the item, until y
## Possible
- DFS explores as far as possible along each branch before backtracking, using a stack data structure.